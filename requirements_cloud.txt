# Cloud Deployment Requirements
# Contains packages needed for Streamlit Cloud

streamlit>=1.30.0
pydantic>=2.0.0
google-generativeai>=0.3.0
requests>=2.31.0

# Development only (not needed in cloud)
# pymupdf (for PDF extraction)
